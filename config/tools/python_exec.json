{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Execute Python code in a sandboxed environment. Use this for complex logic like loops, conditionals, and multi-step operations. You have access to 'device' (offline) or 'device_manager' (online) to call skills.",
  "properties": {
    "code": {
      "type": "string",
      "description": "Python code to execute. Has access to 'device' or 'device_manager' object for skill calls, and 'print()' for output."
    }
  },
  "required": ["code"],
  "additionalProperties": false
}
